Instructions for Funding Circle code challenge:

1. print_table.rb is the script to output the required table on command line
   for its usage, try: ruby print_table.rb -h, 
   for example:
[mmeng@localhost my_primes]$ ruby print_table.rb -h
    Usage: ruby print_table.rb <start_number> <total_number>
           <start_number>: the starting number, must be an integer.
           <total_number>: total numbers to be outputed, must be an integer.

2. for unit test, I just implemented two samples for demo purpose.  
   to test it, try: rake test
[mmeng@localhost my_primes]$ rake test

 
 
